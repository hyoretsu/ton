import BottomBar from '@components/BottomBar';

import { Container } from '@styles/Home';

const Home: React.FC = () => {
    return (
        <>
            <Container />
            <BottomBar />
        </>
    );
};

export default Home;
