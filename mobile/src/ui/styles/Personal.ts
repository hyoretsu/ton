import styled from 'rn-css';

export const Container = styled.View`
    flex: 1;

    max-width: 50%;

    margin: 0 auto;
`;

export const Title = styled.Text`
    align-self: center;

    font-size: 32px;
    font-weight: 500;
    color: #889ae8;

    margin: 12px 0;
`;
